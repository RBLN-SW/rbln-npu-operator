apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    {{- include "rbln-npu-operator.labels" . | nindent 4 }}
  name: {{ include "rbln-npu-operator.fullname" . }}-secret-reader
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
