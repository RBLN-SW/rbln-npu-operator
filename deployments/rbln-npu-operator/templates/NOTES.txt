Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

To view the RBLNClusterPolicy custom resource:

  $ kubectl get rblnclusterpolicy
{{- if .Values.crds.upgrade.enabled }}

The operator CRDs are kept in sync automatically on each `helm upgrade` by a
pre-upgrade hook Job (disable with crds.upgrade.enabled=false).
{{- end }}

For more information, visit: https://github.com/rebellions-sw/rbln-npu-operator
