name: PR Validation

on:
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions:
  contents: read
  pull-requests: write
  statuses: write
  security-events: write # image-scan.yaml uploads SARIF to code scanning

jobs:
  pr-title-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Validate PR title
        uses: amannn/action-semantic-pull-request@v5
        with:
          types: |
            build
            chore
            ci
            docs
            feat
            fix
            perf
            refactor
            revert
            style
            test
          scopes: |
            .*
          requireScope: false
          subjectPattern: ^.{1,100}$
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  manifests-check:
    needs: pr-title-validation
    uses: ./.github/workflows/manifests-check.yaml
  code-check:
    needs: pr-title-validation
    uses: ./.github/workflows/go-checks.yaml
  go-tests:
    needs: pr-title-validation
    uses: ./.github/workflows/go-tests.yaml
  go-build:
    needs: pr-title-validation
    uses: ./.github/workflows/go-build.yaml
  olm-bundle-check:
    needs: pr-title-validation
    uses: ./.github/workflows/olm-bundle-check.yaml
  image-scan:
    needs: [code-check, go-build]
    uses: ./.github/workflows/image-scan.yaml
    secrets: inherit
    with:
      fail-on-findings: true
  # E2E tests (container / vm-passthrough) and the PR image build moved to the
  # Buildkite PR CI pipeline (.buildkite/pipeline.pr.yml). The Go e2e suite under
  # test/e2e/ is kept and still runnable via `make -C test/e2e e2e-test`.
